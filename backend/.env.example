# Backend - copy to .env and fill in values
# Port (default 8001)
PORT=8001

# development | production
NODE_ENV=development

# MongoDB connection string (e.g. MongoDB Atlas). Required in both development and production.
MONGODB_URI=

# JWT secret - use a strong random string (e.g. openssl rand -hex 32)
ACCESS_TOKEN_SECRET=
